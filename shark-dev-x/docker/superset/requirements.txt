# ============================
# Cleaned Superset extra requirements (do NOT include apache-superset or mysqlclient here)
# ============================

# Core runtime helpers (kept minimal)
click>=8.1.3,<9.0
Jinja2>=3.1.2,<4
itsdangerous>=2.1.2,<3.0
Werkzeug>=2.3.0,<3.2

# SQLAlchemy stack (compatible ranges)
SQLAlchemy>=1.4.0,<2.0
SQLAlchemy-Utils>=0.38.0,<0.42

# (De)serialization
marshmallow>=3.20.0,<4.0
marshmallow-sqlalchemy>=0.27.0,<3.0

# Utilities
python-dateutil>=2.8.1,<3.0
PyJWT>=2.0.0,<3.0
Babel>=2.11.0,<3.0
colorama>=0.4.4,<0.5
jsonschema>=3.2.0,<5.0
apispec>=6.0.0,<7.0
pytz>=2022.7
pygments>=2.13.0

# Webframework
flask>=2.3,<3.0
flask-appbuilder>=4.4.0

# Runtime extras for enterprise features
python-dotenv
gunicorn
redis
celery

# Data + Viz (only if you use them)
pandas>=2.0.0
numpy>=1.26.0
pyarrow
pillow

# Drivers you prefer to use as wheels at runtime:
# NOTE: mysqlclient removed here on purpose to avoid pip trying to build
# psycopg2-binary may be installed in the Dockerfile as binary wheel
psycopg2-binary
pymysql
pymongo
mongoengine

# Forms & utils
WTForms>=3.0.0
flask-cors
markupsafe>=2.1.0
